# Adhera Angular App

Placeholder for the Angular frontend (patient + caregiver dashboards).

Scaffold with:

```bash
npx @angular/cli new adhera-app --directory . --routing --style scss
```

Requirements: Angular Material, responsive layout, large accessible controls, web push support. See `../../PROJECT_SPEC.md` (Frontend section).
